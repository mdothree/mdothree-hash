{
  "name": "mdothree-hash",
  "version": "1.0.0",
  "description": "Hash generator utility \u2014 mdothree.com",
  "scripts": {
    "dev": "node scripts/inject-config.js && npx serve public",
    "build": "node scripts/inject-config.js",
    "start": "npx serve public",
    "test": "node --test --experimental-vm-modules tests/hashGenerator.test.js"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  },
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  }
}